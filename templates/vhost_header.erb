server {
	listen		<% if @listen_ip %><%= @listen_ip %>:<% end %><%= @listen_port %> <% if @listen_options %><%= @listen_options %><% end %><% if @default_server %> default_server<% end %>;
	server_name	<%= @server_names.join(" ") %>;
	access_log	/opt/www/sites/<%= @config_name %>/logs/<%= @access_log_filename %> <%= @log_format %>;

<%= @extra_server_config %>

	<% if @ssl == true %>
	ssl				on;
	ssl_certificate			/opt/ssl/<%= @config_name %>/<%= @name %>.crt;
	ssl_certificate_key		/opt/ssl/<%= @config_name %>/<%= @name %>.key;

	# ssllabs recommendation
	ssl_protocols TLSv1 TLSv1.1 TLSv1.2;
	ssl_prefer_server_ciphers on;
	ssl_ciphers "EECDH+ECDSA+AESGCM EECDH+aRSA+AESGCM EECDH+ECDSA+SHA384 EECDH+ECDSA+SHA256 EECDH+aRSA+SHA384 EECDH+aRSA+SHA256 EECDH EDH+aRSA !aNULL !eNULL !LOW !3DES !MD5 !EXP !PSK !SRP !DSS !RC4";

	# should support about 40K unique sessions in 10 min
	ssl_session_cache shared:SSL:10m;
	ssl_session_timeout 10m;
	<% end %><% if @redirect_url %>return 301<% if @redirect_https %> https://<% else %> $scheme://<% end %><%= @redirect_url %>$request_uri;<% end %>
